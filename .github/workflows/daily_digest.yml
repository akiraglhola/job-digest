name: Job Digest Diario

on:
  schedule:
<<<<<<< HEAD
    - cron: "0 6 * * 1-5"
  workflow_dispatch:
  
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
=======
    # Lunes a viernes a las 8:00h hora peninsular (UTC+2 en verano = 06:00 UTC)
    - cron: "0 6 * * 1-5"
  workflow_dispatch:
    # Permite lanzar el workflow manualmente desde la interfaz de GitHub
>>>>>>> 4cc87a2 (adding Github Actions cron workflow)

jobs:
  digest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Ejecutar Job Digest
        env:
          ANTHROPIC_API_KEY:  ${{ secrets.ANTHROPIC_API_KEY }}
          GMAIL_USER:         ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          RAPIDAPI_KEY:       ${{ secrets.RAPIDAPI_KEY }}
          MIN_SALARY:         ${{ secrets.MIN_SALARY }}
          LOCATIONS:          ${{ secrets.LOCATIONS }}
          ROLES:              ${{ secrets.ROLES }}
          TECH_STACK:         ${{ secrets.TECH_STACK }}
<<<<<<< HEAD
        run: python digest.py
=======
        run: python digest.py
>>>>>>> 4cc87a2 (adding Github Actions cron workflow)
